# Railway Database Configuration
# Option 1: Railway MongoDB (if you add MongoDB service)
MONGO_URL="mongodb://mongo:27017/gili_db"
# Option 2: Railway PostgreSQL (recommended - add PostgreSQL service and use with MongoDB adapter)
# MONGO_URL="postgresql://user:password@host:port/database"
# Option 3: External MongoDB Atlas (recommended for production)
# MONGO_URL="mongodb+srv://username:password@cluster.mongodb.net/gili_db?retryWrites=true&w=majority"

DB_NAME="gili_production"

# Railway Runtime Configuration
PORT=8001