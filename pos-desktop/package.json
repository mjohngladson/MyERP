{
  "name": "gili-pos",
  "version": "1.0.0",
  "description": "GiLi Point of Sale Desktop Application",
  "main": "production-main.js",
  "scripts": {
    "start": "electron production-main.js --no-sandbox --disable-dev-shm-usage",
    "dev": "NODE_ENV=development electron main.js --dev --no-sandbox --disable-dev-shm-usage",
    "build": "electron-builder --config build-config.js",
    "build-win": "electron-builder --win --config build-config.js",
    "build-portable": "electron-builder --win portable --config build-config.js",
    "build-all": "electron-builder --win --linux --config build-config.js",
    "clean": "rm -rf dist/ && rm -rf node_modules/ && npm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "electron-builder install-app-deps"
  },
  "keywords": ["pos", "point-of-sale", "gili", "electron", "offline"],
  "author": "GiLi Team",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.1.0",
    "electron-builder": "^24.9.1"
  },
  "dependencies": {
    "sqlite3": "^5.1.6",
    "axios": "^1.6.2",
    "electron-store": "^8.1.0",
    "node-thermal-printer": "^4.4.4",
    "serialport": "^12.0.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "build": {
    "appId": "com.gili.pos",
    "productName": "GiLi PoS",
    "directories": {
      "output": "dist"
    },
    "files": [
      "**/*",
      "!node_modules/**/*",
      "node_modules/sqlite3/**/*"
    ],
    "win": {
      "target": "nsis",
      "icon": "assets/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}